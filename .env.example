NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only — never expose to the client. Used exclusively by API routes
# under app/api/** that need elevated privileges (e.g. deleting a user).
SUPABASE_SERVICE_ROLE_KEY=